#include "main.h"
/**
 *print_numbers: -it loops through number from 0 -9
 *@i: -initialization
 *Returns: void
 */
void print_numbers(void)
{	
	for (int i = 0, i <= 9, i++);

	putchar(i);
	putchar(\n);
}
